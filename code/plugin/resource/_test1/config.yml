---

description: "test resource-plugin #1"

start:
  exec: ['/bin/bash', 'start.sh']  # start the resource
  args: 0

stop:
  exec: ['/bin/bash', 'stop.sh']  # start the resource
  args: 0

active:
  exec: ['/bin/bash', 'active.sh']  # check if the resource is active right now
  args: 0

other:
  exec: ['/bin/bash', 'other.sh']  # check if the resource is active on another node
  args: 0

modes:
  valid:
  - 'standalone'
  fallback: 'standalone'
