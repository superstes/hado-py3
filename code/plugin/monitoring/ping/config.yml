---

description: "-t: the ping target, -s: ping source address (optional), -w: timeout in seconds (optional, pe: '-w=0.3', default=0.2)"

check:
  exec: ['bash', 'check.sh']  # start the monitoring check
  args: 1
